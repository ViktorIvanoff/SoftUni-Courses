function projectsCreation(arg1, arg2) {

    let name = arg1
    let projects = Number(arg2)
    let result = Number(projects * 3)

    console.log(`The architect ${arg1} will need ${result} hours to complete ${projects} project/s.`)

}